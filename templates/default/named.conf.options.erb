options {
    directory "/var/cache/bind";
    recursion no;
    notify-to-soa yes;
    notify master-only;

    auth-nxdomain no;    # conform to RFC1035
    listen-on-v6 { any; };
    allow-transfer { none; };
    querylog yes;
};
